version: '3.8'

services:
  api-gateway:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - microservices-net

  # Placeholder for frontend-next
  # Placeholder for frontend-admin
  
  # User Service (NestJS + PostgreSQL)
  # postgres:
  #   image: postgres:15
  #   ...

  # Scraper Service (Go + MongoDB)
  # mongodb:
  #   image: mongo:6
  #   ...

  # Resume Service (FastAPI)
  
  # Notification Service (Express)

networks:
  microservices-net:
    driver: bridge
